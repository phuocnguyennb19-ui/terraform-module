# Terraform
.terraform/
# NOTE: .terraform.lock.hcl is deliberately NOT ignored — it pins provider
# versions and checksums so CI resolves exactly what you resolved. Commit it.
*.tfstate
*.tfstate.backup
*.tfvars
*.tfvars.json
tfplan
tfplan.json
tfplan-*
*.tfplan
*.tfplan.json

# Logs
*.log

# OS
.DS_Store
